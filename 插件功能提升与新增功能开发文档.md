# Jev X & Weibo Reader 功能提升与新增功能开发文档

评审日期：2026-09-21  
代码基线：`package.json` / `manifest.json` 中的 v0.2.0  
文档性质：基于现有实现的开发建议与分期方案；本文中的新增模块、接口、指标均为建议，尚未实现。

## 1. 建议先做什么

当前插件已经形成“信息流提取 → Jev 多维评分 → 颜色提示 → 用户点击深入解析 → 可选联网研究”的完整路径。下一阶段最有价值的方向是：让评分始终对应当前正文、让请求与额度可控、让高价值内容能够留存和再次利用。

建议按以下顺序推进：

1. **优先补齐可靠性**：内容版本与缓存一致性、跨标签页请求调度、深度解析任务状态、会话上下文恢复。
2. **改善日常阅读体验**：紧凑评分卡、按分数停留或跳转、可视化偏好设置、深度解析进度与取消。
3. **增加持续使用价值**：本地收藏与阅读清单、手动反馈、主题档案、来源可核对的专题摘要。

第一轮不建议同时扩展大量网站或引入云端账号体系。现有 X、微博适配和任务链路更值得先做稳。

## 2. 评审范围与现有能力

本次查看了 README、原技术设计、Manifest、设置页、两个 DOM Adapter、content script、自动浏览、浮层、后台调度、缓存、Provider 及测试。原有 `jev_x_reader_technical_design.md` 保留不动；实际是否已经实现，以源代码为准。

| 能力 | 当前实现 | 后续提升空间 |
| --- | --- | --- |
| 平台支持 | X/Twitter 与桌面微博；以 `article` 为主要提取容器 | 更多真实页面样本、长帖与上下文完整度提示 |
| 快速评分 | 单次 Jev 请求包含多个维度，本地确定性计算综合分 | 评分解释、反馈、按阅读场景切换偏好 |
| 价值提示 | 彩色层、维度分数、低置信度保护、显示原文 | 更紧凑的默认展示、深色模式、低分折叠 |
| 自动浏览 | 固定停留、交互暂停、隐藏标签暂停、Session 上限 | 分数驱动停留、可配置恢复、读完位置与无进展停止 |
| 深入解析 | 手动触发，OpenAI-compatible / Ollama，非流式右侧面板 | 缓存、去重、取消、分阶段进度、追问 |
| 联网研究 | Tavily 可选，来源列表、编号引用、失败降级 | 引用校验、搜索意图提取、来源质量与时效提示 |
| 缓存 | Jev 本地缓存最多 500 条、7 天 | 区分推理和展示配置、增加字节容量约束 |
| 原文上下文 | 会话存储最多 120 条，写入时清理超 24 小时项 | 上下文淘汰后可在当前页重建 |
| 设置 | 偏好、阈值、服务参数；评分维度编辑 JSON | 可视化编辑、预设、无密钥导入导出 |
| 内容留存 | 面板能复制解析文本 | 收藏、标签、阅读清单、完整 Markdown 导出 |

### 本次验证结果

- `npm run check`：通过。
- `npm test`：27 项通过、0 项失败。
- 未运行真实 API 测试，未加载浏览器验证线上 X/微博页面，未执行构建覆盖 `dist`。
- 本文将静态代码可确认的缺口与需要浏览器复现的风险分开描述；测试通过不等于动态信息流和 MV3 生命周期均已验证。

## 3. 优先级与工作量

P0 表示下一版优先解决的正确性或资源控制问题；P1 表示高价值体验提升；P2 表示基础稳定后的扩展。工作量为单人熟悉当前代码后的粗估，包含相关测试，不包含等待第三方接口或平台改版的时间；各项可能共享基础工作，不宜直接相加作为承诺工期。

| 编号 | 项目 | 优先级 | 预估人日 | 依赖 |
| --- | --- | --- | --- | --- |
| A1 | 内容版本与分层缓存 | P0 | 2–4 | 无 |
| A2 | 全局队列、预算、失败冷却 | P0 | 3–5 | A1 |
| A3 | 深度解析任务去重与生命周期 | P0 | 3–5 | A1、A2 |
| A4 | 上下文完整度、恢复及长会话清理 | P0 | 2–4 | A1 |
| B1 | 阅读模式与智能自动浏览 | P1 | 3–5 | A2、A4 |
| B2 | 可视化设置与主题档案 | P1 | 2–4 | A1 |
| B3 | 深度解析进度、流式输出和追问 | P1 | 4–6 | A3 |
| B4 | 联网证据与引用质量 | P1 | 3–5 | A3、A4 |
| C1 | 本地收藏、阅读清单与导出 | P1 | 3–5 | A1、A4 |
| C2 | 兴趣反馈与本地规则 | P1 | 2–4 | B2、C1 |
| C3 | 专题聚合与阅读摘要 | P2 | 4–7 | C1、B4 |
| C4 | 按需图片理解与翻译 | P2 | 3–5 | A2、A3、A4 |
| C5 | X 转发原帖、站内文章与外链全文读取 | P1 基础 / P2 动态页面增强 | 基础 4–6，增强另需 3–5 | A1–A4、B4 |
| D1 | 本地诊断与浏览器回归体系 | P0 起持续建设 | 2–4 起 | 随 A/B/C 增量建设 |

## 4. P0：优先修复与加固

### A1. 内容版本与分层缓存

**已确认的代码现象**

- `src/content/index.js` 中 `resultById` 和 `inFlightById` 按 `postId` 缓存。`registerArticle()` 即使提取到同一帖的新正文，仍可能直接渲染旧结果；`analyzeForPost()` 也先查同一 ID 的结果。
- 后台 `analyzePost()` 的缓存包含 `state`，能区分内容；但前台命中旧结果时不会调用后台。需要修复的是两层缓存契约不一致。
- `applyConfig()` 对整个公开配置做比较，任何变化都会清空前台结果。显示阈值或停留时间的变化不应使已有评分卡回到等待状态。
- 后台缓存键包括完整 `config.questions`，其中含评分权重；`buildJevState()` 又包含互动数量。权重调整或点赞数更新可能导致不必要的重新推理。

**开发方案**

引入独立版本：`contentHash` 表示规范化正文、引用、媒体文字和已确认上下文；`analysisConfigHash` 表示模型、阅读偏好和实际提交的问题定义；本地权重/阈值使用单独的评分或展示版本。

- 前后台统一用 `postId + contentHash + analysisConfigHash` 定位推理结果；后台自行重算哈希，不信任页面提供的缓存身份。
- 异步结果返回时比较内容版本和配置版本；旧结果不覆盖新正文、复用后的 DOM 节点或新主题档案。
- 原始维度结果与综合分分离保存；修改权重只调用本地计算，修改颜色只重绘。
- 互动数默认仅展示，不参与推理指纹；若保留“参考热度”模式，则明确标注并按时间窗口更新。
- 旧缓存加 `schemaVersion`，无法可靠迁移时自然失效；不清除密钥和用户偏好。

**验收**

1. 同 ID 正文从截断变完整时产生新内容版本；延迟返回的旧响应不能覆盖新响应。
2. 同内容重复出现不重复请求；同一 DOM 从帖子 A 复用为 B 后不显示 A 的分数或解析。
3. 仅调整权重、颜色或停留时间，Jev 请求数不增加；修改问题文本或兴趣才重新判断。

### A2. 全局队列、预算与失败冷却

**现状与风险**

后台 Jev 已有并发限制和最多 50 个待处理任务，但每分钟限流按 `tabId` 分桶，保存在内存。多个标签页可以分别使用完整额度，worker 重启也会丢失计数。`maxPostsPerSession` 只约束自动浏览循环，视口自动评分不受这一计数直接约束。深度解析目前没有同等的全局队列和预算控制。

这并不等于现有上限完全无效，但设置文案容易让用户误以为它是全插件的硬上限。

**开发方案**

- 提供“本标签页阅读上限”和“全插件外部请求上限”，明确两者含义。
- 后台统一调度 Jev、Tavily、LLM，分别设并发和预算。优先处理用户点击，其次视口内，最后预取；对不同标签页公平调度，避免一个页面占满队列。
- 分别记录逻辑任务数、实际网络尝试数、缓存命中；重试也经过请求预算，连接测试明确计入对应统计。
- 会话级限流计数保存在 `storage.session`，日级使用量保存在本地；串行执行“额度预留—提交”，防止并发穿透。
- 429/网络故障提供冷却和有限重试；退出页面、内容失效或禁用后取消尚未开始的任务。
- UI 展示排队、冷却和额度状态；缓存命中仍可使用。费用只能基于服务返回用量及用户配置单价估算，不硬编码为真实账单。

**验收**

使用模拟 Provider 在三个标签页同时提交任务；全局并发和请求数不越界。重启 worker 后窗口计数仍有效。达到额度后新网络请求停止，已缓存结果仍可展示；关闭自动浏览不应被误解为关闭所有视口评分。

### A3. 深度解析任务去重、缓存与生命周期

**已确认的代码现象**

`DEEP_ANALYZE` 每次后台调用都会重新走搜索/LLM 链路。单张卡片虽然会禁用正在执行的按钮、保留局部显示结果，但跨标签页或页面重建没有共享深度缓存。该分支也没有像 `analyzePost()` 一样检查 `config.enabled`。关闭面板没有取消网络请求的协议；多个帖子的响应先后到达时，面板归属需要专项回归。

**开发方案**

- 将深入解析变为任务：`queued → searching → generating → completed / failed / cancelled / interrupted`；分配 `jobId`，绑定帖子、内容版本、配置版本和面板订阅者。
- 缓存键覆盖内容、模型/服务配置标识、提示词版本、兴趣、搜索选项；不包含明文 API Key。切换凭据可通过配置版本隔离。
- 推荐默认深度结果保留在有界会话缓存；持久保存只由收藏动作触发。联网结果另设有效期和检索时间，提供“重新联网分析”。
- 同键任务合并；取消单个订阅不影响其他用户界面，最后一个订阅取消时再中断共享网络请求。
- 提供明确“取消”按钮；关闭面板只隐藏，任务是否继续需有状态提示。禁用插件阻止新分析，清理排队任务，并尽力中止运行任务；不能承诺撤回已到达服务商的请求。
- worker 重启后，未完成任务标为 `interrupted`，由用户选择重试，避免不透明地再次计费。不要把在内存中的 Promise 当作可恢复任务。
- `POST_CONTEXT_MISSING` 时由当前页面重新提供可见正文，后台重新校验来源和内容版本，不强迫用户整页刷新。

Chrome 官方说明 worker 可能终止，且全局变量会丢失；因此持久化的是任务元数据和可恢复状态，而非假设内存任务永久存在。参见 [扩展 service worker 生命周期](https://developer.chrome.com/docs/extensions/develop/concepts/service-workers/lifecycle)。

**验收**

同一帖子在两个标签页触发，仅产生一份等价任务；取消后不再更新已取消面板。先打开 A 再打开 B，A 的晚到结果不得抢占 B。禁用后不能从残留按钮发起新请求。worker 中断有明确状态，刷新页面能使用仍有效的缓存。

### A4. 提取完整度、上下文恢复与长会话稳定性

**已确认的代码现象**

- 两个 Adapter 主要采集当前 DOM；正文截取最多 12,000 字符、引用最多 6,000 字符，未输出完整度状态。
- Core 和深度提示支持 `visibleThreadContext`，但两个 Adapter 当前都没有实际填充该字段。
- 媒体主要是 alt/aria 文本，不能等价于读懂图片、视频。
- 观察器监听 `childList`，没有监听 `characterData`；只处理新增节点，纯文本变更及仅删除节点的清理需要补齐。
- `resultById` 没有容量淘汰；脱离文档的文章清理依赖后续扫描，长时间滚动需检验内存是否持续增长。
- 会话上下文最多 120 条，而 Jev 缓存可有 500 条；旧卡片存在不代表后台还保留可解析的原文。

**开发方案**

增加 `extractionQuality`：正文是否疑似折叠、是否达到长度上限、媒体仅有 alt、线程上下文是否缺失、Adapter 版本。无法判断完整性时标为未知，不标为完整。

仅沿明确的回复/线程关系组织当前可见上下文，不能把相邻推荐帖拼成对话。用户主动展开正文后重新提取；暂不自动翻页或批量点击。对图片主导的帖子提示“未读取图片内容”。

对纯文本变更增加防抖观察，过滤扩展自身 DOM；处理删除节点、取消 observer 订阅和过时任务；给页面结果 Map 设置容量或 LRU。更细的 hash 规则需防止互动数频繁变化触发网络请求。

存储同时控制条目数和估算字节数。当前最低 Chrome 版本为 102，容量不能只按新版本假设；按官方存储说明做版本兼容和 quota 错误降级，不让“写缓存失败”吞掉已经成功的模型结果。参见 [Chrome Storage API](https://developer.chrome.com/docs/extensions/reference/api/storage)。

**验收**

长帖展开、引用更新、纯文本更新和仅删除节点分别有用例；121 条以上已分析帖子中，旧帖仍可在用户点击时重建上下文。模拟滚动 1,000 条帖子后 Map 大小受控，无持续保留脱离 DOM 的文章引用。存储超限时仍展示本次成功结果并给出缓存提示。

## 5. P1：让日常阅读更顺手

### B1. 阅读模式与分数驱动自动浏览

**用户价值**：把分数用于实际阅读决策，减少评分卡对原文的遮挡。

- 默认紧凑角标，仅展示综合分、价值标签和不确定状态；展开后再显示维度。
- 三种模式：原文优先、评分辅助、专注阅读。专注模式允许折叠低分帖子，保留作者、分数、折叠原因和恢复按钮；低置信度不自动折叠。
- 自动浏览使用可调策略，例如低分短停、中分普通停、高分长停或等待用户继续。具体阈值复用当前设置，不另建冲突体系。
- “暂停后自动恢复”与“手动继续”可选择；用户查看深度解析或持续选中文本期间不自动抢走阅读位置。
- 增加“下一条高价值”“回到上一条”“本次已读数”。无新帖子或滚动位置长期无变化时停止并说明原因。
- 支持深色模式、键盘聚焦和文本标签；颜色不作为唯一信息来源。

落点：`auto-scroller.js`、`overlay.js`、`content/index.js`。验收覆盖低置信度保护、选中文本、面板打开、隐藏标签、无内容可滚动与手动恢复；原文链接和平台按钮仍能正常使用。

### B2. 可视化设置与主题档案

**用户价值**：不需要编辑 JSON 就能让插件符合不同阅读目标。

- 将默认维度变成可视化表单：开关、说明、权重、判断标准；保留高级 JSON 模式，两个入口共用校验。
- 提供“技术研究”“行业资讯”“轻量浏览”起始模板，明确其为可编辑示例，不承诺通用最优。
- 支持多个命名档案，按平台设置默认档案；跨平台是否使用同一兴趣由用户选择。
- 用已有维度结果实时预览调权效果，不调用 API；完整解释基础加权分及营销扣减。当前营销因子是乘法，不应写成固定扣分。
- 提供设置导入导出、恢复默认、字段级错误；导出默认剔除全部密钥，导入校验 schema，不覆盖未包含的凭据。
- 简化首次配置流程：先完成 Jev 即可评分，LLM/Tavily 后续可选配置。

落点：`options/*`、`shared/core.js`。验收：可视化与 JSON 往返不丢字段；更改展示项不产生推理；主题切换不串用旧兴趣结果；导出文件中不存在 Key。

### B3. 深度解析体验、流式结果与有限追问

先实现“排队、检索、生成、完成”进度，再按服务能力加入流式输出。当前 `llm-router.js` 固定 `stream: false`，需要独立 SSE 解析器与消息协议；支持非流式回退，避免假定所有兼容服务都正确实现流式。

建议使用 `runtime.connect` 传递 `jobId`、递增 `sequence` 和状态事件，处理断线、分片 JSON、多行事件、UTF-8 边界及取消。端口存在不等于任务永不终止，仍遵守 A3 的中断策略。

深度解析模板由当前单一技术分析扩展为“技术解析、事实核对、快速摘要、术语解释”。提示词应根据平台和模式生成，修正目前固定写为 X/技术研究助手的表述。

追问首版限制在当前帖子、已有结果和来源；默认不再次联网，用户可以明确选择重新搜索。限制历史轮数与上下文长度，显示本次是否检索。Markdown 安全渲染保持当前文本安全边界，不引入可执行 HTML。

验收：首批文字可增量显示；切换帖子不会混入前一帖内容；不支持流式的服务可回退；取消立即停止界面追加；输出过长要明确截断提示，避免当前面板 16,000 字符截取造成无提示缺失。

### B4. 联网证据与引用质量

**现状**：后台将作者与正文合并、去 URL 后截取 500 字符作为查询。搜索结果编号后交给 LLM，提示词要求正确引用，但没有程序级引用检查。搜索摘要只提供线索，不等于阅读全文或完成事实核验。

**开发方案**

- 从正文提取核心实体和待核实主张，允许用户编辑查询；初期以规则提取为主，模型改写只作显式可选步骤。
- 来源记录 URL、标题、检索时间、摘要；发布时间不存在时标未知，不推断。
- 优先展示原始发布、论文、官方说明等一手来源，但域名权威性不自动等于结论正确；URL 去重，保留原始归属。
- 先完成确定性的引用检查：回答中的 `[Sx]` 必须存在于本次来源表；无效引用显示“引用不可用”，不能生成虚构链接。
- 将结论分成原帖陈述、外部来源支持、推断、待核实；来源矛盾时并列展示。
- 增加“仅原帖解析/联网研究”单次选择。搜索失败或无结果时明确标为未完成外部验证。

验收：虚构 `[S99]` 被识别；多个来源指向同一 URL 时去重；空搜索结果不得显示“已核实”；复制/导出携带来源 URL 与检索时间。自动引用校验只保证编号存在，不能声称保证事实正确。

## 6. 值得新增的功能

### C1. 本地收藏与阅读清单（最建议首批新增）

目前高分内容离开信息流后不容易找回。增加“收藏/稍后读”，保存原帖链接、平台、作者、正文快照、评分版本、可选深度解析、来源、备注和标签。

首版使用扩展内部独立页面，支持未读/已读、标签、平台、日期筛选和本地关键词搜索。不要直接重排宿主信息流 DOM。明确区分“用户收藏”与“可自动淘汰的缓存”，清理缓存不得删除收藏。

提供单帖和选中集合的 Markdown 导出，包含原帖 URL、正文快照时间、分析时间和引用清单。选择保存正文时说明这是本地快照，原帖删除不会自动删除该快照；支持按条删除与清空。首版可设 500 条软上限并同时校验存储字节容量，超限提示导出或清理。

新增建议：`src/background/library-service.js`、`library/` 页面。更大规模再迁移 IndexedDB。验收：重启浏览器后收藏存在；重复收藏更新同一条；清缓存不删收藏；删除后搜索、索引和详情都不可再读到该条。

### C2. 兴趣反馈与可解释的本地规则

增加“有价值/不感兴趣/评分不准”，可附原因：主题不符、营销、重复、内容不完整等。反馈先本地保存，并显示其作用范围。

第一版只提供可查看、可撤销的作者/关键词/主题规则。使用平台稳定作者身份，微博昵称不能作为唯一作者 ID。命中规则时显示理由，原始 Jev 分数与本地调整分分开。用户可选择“仅本地筛选，命中即跳过 API”。

积累反馈后可生成“建议调整偏好”的预览，由用户应用，不静默改写兴趣。验收：规则命中可解释；一键撤销恢复原排序与显示；不同档案互不污染；未授权时反馈不上传任何服务。

### C3. 专题聚合与按需阅读摘要

对用户收藏或明确选择的帖子进行主题归组，生成“本专题有哪些新信息、哪些观点重复、哪些结论相互矛盾”。首版先做同 URL、同规范化文本去重，再考虑语义聚类。

摘要由用户按需生成，显示输入条数并允许排除，不默认上传整段浏览历史。每条结论关联原帖和可用外部来源，标明它反映的是所选阅读集合，不代表全网趋势。

只有在本地收藏使用稳定、来源映射可验证后再投入这项。验收：每个摘要要点可回到选中集合中的具体帖子；没有支持材料的要点不能被表述为确定事实。

### C4. 按需图片理解与翻译

优先做轻量的“翻译此帖/解释术语”，复用深度任务框架与缓存。图片理解只在点击后提交明确选中的图片，限制数量和大小，显示新增服务和预计输入量；请求失败不影响纯文本解析。

不把截图中看似存在的细节当作已确认事实。外链全文读取独立列为 C5，与图片理解分开开发和验收。

该项可能涉及新的模型能力、权限和成本，故安排在 P2。验收：未点击时无新增图片请求；不支持多模态时明确降级；图片无法读取不会产出“已读取图片”的结论。

### C5. X 转发原帖、站内文章与外链全文读取

补充日期：2026-09-21。结论基于当前代码及 Chrome 官方扩展机制，尚未以真实 X Articles、登录页面和外链样本验证提取成功率。

#### C5.1 能力边界与当前实现

**浏览器扩展具备获取这些内容的能力，但当前插件只实现了部分页面文字提取，没有通用的链接全文读取链路。** 必须区分“发现链接”“取得预览摘要”和“读取目标正文”，避免将帖子预览或搜索摘要当成全文。

| 类型 | 当前代码实际行为 | 需要补充的能力 |
| --- | --- | --- |
| 普通转发 Repost | 原帖在信息流以匹配的帖子 DOM 展示时，按原帖提取正文；没有独立转发关系模型 | 保留原帖身份与转发关系；正文不完整时读取详情 |
| 引用转发 Quote | `readQuote()` 提取引用作者和文字，最多 6,000 字符；主帖与引用分开 | 保存引用帖 URL/ID，按需补齐引用正文，不无限递归引用链 |
| 普通长帖 | 提取当前 DOM 中的正文，最多 12,000 字符 | 检测折叠/截断，展开或进入详情后刷新内容版本 |
| X Articles 站内文章 | 没有专门的正文 Adapter；不能保证读取全文 | 识别文章入口，在文章页加载后提取标题、作者、正文与原文链接 |
| 帖子中的站外文章 | 没有独立外链集合和目标正文抓取器；主帖 URL 仅用于定位帖子 | 收集外链、解析跳转、请求目标页面、抽取正文 |
| 回复串 Thread | Core 接受 `visibleThreadContext`，当前两个 Adapter 没有填充 | 仅采集有明确关系的可见回复，不将相邻推荐内容当成线程 |

代码依据：`src/content/x-dom-adapter.js` 的 `findPermalink()`、`readQuote()`、`extract()`；`src/shared/core.js` 的 `validateExtractedPost()`；`src/background/service-worker.mjs` 的 `buildDeepAnalysisContext()`。

当前 Tavily 调用 `/search`，并设置 `include_raw_content: false`；后台搜索查询还会移除正文 URL。因此，现有“联网研究”不能作为“已经读取帖内指定文章”的证明，必须与 C5 的定向正文读取分开标记。

#### C5.2 用户交互

在检测到引用帖或文章链接时，显示“读取引用原帖”或“读取链接全文”。多个链接先列出标题/域名供用户选择，不默认批量访问。

操作流程：

1. 用户选择目标，界面显示即将读取的链接和获取方式；需要时申请对应网站权限。
2. 任务进入“获取页面 → 提取正文 → 完成/部分完成/失败”，提供取消。
3. 成功后显示正文来源、最终 URL、提取时间、字符数和完整度；预览正文后可选择“使用全文重新评分”或“结合全文深入解析”。
4. 获取失败时保留原帖分析，明确标注“仅依据原帖/引用预览”，不显示已读全文。

首版正文获取本身不调用模型、不自动产生额外模型费用。只有用户继续分析时才将选中正文交给相应服务；请求仍经过 A2 的调度和预算。

#### C5.3 获取方案

**路径一：读取已经加载的页面 DOM。** 用于当前 X 原帖、引用详情、站内 Articles，以及用户已打开且授权访问的外部文章页。复用浏览器实际可见内容与登录状态，等待目标正文容器加载，再通过对应 Adapter 提取。不读取账号凭据，不假定“能打开页面”就必然已经加载全文。

**路径二：扩展后台请求 HTML 并提取正文。** 适用于返回静态正文的外部文章。取得目标域名授权后，由 service worker 请求页面；将 HTML 交给扩展内部具备 DOM 解析环境的页面进行正文抽取，worker 本身没有 `DOMParser`。可使用专用提取页面，若后续采用 offscreen document，则另行核对最低 Chrome 版本与权限；当前最低版本为 102，不能直接假设所有新增 API 可用。解析过程不执行目标脚本或加载子资源，输出纯文本及必要元数据。

**路径三：打开目标页面后提取。** 路径二遇到仅有应用壳、动态加载正文或登录态要求时，提示用户打开目标页并读取。首版不自动创建隐藏标签页批量抓取。若后续支持插件创建临时标签页，应管理加载超时、取消及标签页归属，只关闭插件自行创建的页面。

扩展后台在获得主机权限后可以发起跨域请求；content script 中的跨域请求仍受页面同源策略约束。动态页面可通过获得授权的 content script 读取 DOM。官方依据：[跨域网络请求](https://developer.chrome.com/docs/extensions/develop/concepts/network-requests)、[Content scripts](https://developer.chrome.com/docs/extensions/develop/concepts/content-scripts)。这些机制提供实现基础，不保证网站一定返回可用正文。

#### C5.4 URL、权限和正文处理

- 从帖子正文和链接卡片提取候选链接，区分原帖链接、引用链接、Articles 入口与站外链接；不把点赞、头像、分享等操作链接当成文章。
- 保存 `originalUrl`、`resolvedUrl` 和可验证的正文来源；处理 `t.co` 等短链，但不把短链文本当成最终目标。保留可能影响资源身份的查询参数，不能粗暴删除所有参数。
- 目前 Manifest 声明了广泛的 `optional_host_permissions`，不代表这些网站已经获授权；需实现按域名申请、拒绝处理和已授权检查。现有设置页只针对 LLM 服务申请权限。
- 重定向后的目标也要执行协议、地址和权限校验；不以已授权短链域名代替最终站点授权。若 fetch 跳转机制无法安全确定目标，转为用户打开页面的提取方式。
- 限制为可用的 HTTP(S) 网页目标；拒绝 `javascript:`、`data:`、携带用户名密码的 URL，以及本地/内网目标，避免任意帖文将后台当作通用网络代理。消息处理校验来源及目标与所选帖子的关联。
- 给网页请求设置响应大小、跳转次数、超时和并发限制。登录页、验证页、付费提示及空壳页面返回专门状态，不进入模型的“文章正文”字段。
- 提取标题、作者、发布时间、正文与章节；未知字段留空。去除导航、广告和推荐区域，保留引文所属关系。PDF、图片、视频首版标为不支持，不伪装成 HTML 文章成功结果。
- 正文有长度上限，UI 显示“部分正文/已截断”；DOM 读取也可能不完整，只有具备依据时才标完整。不绕过登录、访问控制或付费限制。

#### C5.5 数据与分析链路

新增建议模块 `src/background/link-reader-service.js`，负责目标校验、权限状态、调度与缓存；`src/content/x-article-adapter.js` 负责 X Articles DOM 提取；通用正文提取放在可使用 DOM 的扩展内部模块。复用 A3 的 `jobId`、取消、去重及中断状态。

建议新增记录：

```text
LinkedContent:
  id, parentPostId, relation (quote / x_article / external_article),
  originalUrl, resolvedUrl, title, author, publishedAt,
  text, contentHash, fetchedAt,
  extractionMethod (page_dom / fetched_html),
  completeness (complete / partial / unknown), truncationReason,
  status, errorCode
```

主帖、引用原帖、站内文章、外部文章和搜索证据分别保留来源，不把所有文字无标识地拼接。传给模型时明确标注这些内容为不可信材料，不执行文章中的指令。

默认 Jev 快速评分仍仅使用当前原帖上下文；用户选择“使用全文重新评分”后，派生新的分析输入。所选全文的 `contentHash`、来源集合与分析模式均纳入 A1 的缓存身份；只修改页面展示不触发新推理。重新获取后正文变化必须生成新版本，并避免旧请求覆盖新结果。

全文默认进入有界会话缓存，仅用户收藏时持久化快照。首次建议每次读取 1 篇、正文输入上限 20,000 字符、缓存最多 50 篇，同时受字节容量上限约束；这些是待验证的配置起点，不是现有实现。达到限制应提示缩减或分段，不能静默截断。收费分析提交前明确显示此次使用的是全文还是截断正文。

#### C5.6 开发分期与验收

**P1 基础**：引用 URL/ID 提取、链接列表、静态外链正文、用户已打开的 X 文章页提取、来源和完整度展示、接入按需深度解析。依赖 A1–A4，预估 4–6 人日；先以真实样本确认 Articles 结构，再确定 Adapter 实现细节。

**P2 增强**：受控的目标页打开与等待、动态外链站点回退、更完整的线程关系及站点适配，另估 3–5 人日；不承诺覆盖所有网站。

| 场景 | 验收结果 |
| --- | --- |
| 普通转发、引用转发 | 主帖与引用作者/正文/URL 不混淆；原帖无法访问时保留可见预览并说明 |
| 折叠长帖、Articles | 展开或打开文章后能提取正文；未加载完整时不能标记已读全文 |
| 静态文章、多个外链 | 只获取用户选中的目标，过滤导航噪声，正文和来源可追溯 |
| 短链与跨域跳转 | 校验最终目标，权限不足明确提示；危险地址不请求 |
| 动态、登录、验证或付费页 | 返回准确状态或引导在页面读取，不把提示页当正文 |
| 权限拒绝、超时、取消、worker 中断 | 有明确终态，保留原帖分析，不发生隐藏重试循环 |
| 重复点击与内容更新 | 等价任务合并；全文变化使相关分析失效，晚到结果不覆盖新版本 |
| 全文过长、非 HTML、存储超限 | 明确标注截断/不支持/缓存失败；没有虚假的全文成功状态 |
| 深入解析 | 输出可区分原帖、定向文章正文和 Tavily 搜索摘要；每个来源可以打开核对 |

浏览器验收至少包含普通转发、引用帖、长帖、X Articles、公开静态文章、动态文章、短链跳转及权限拒绝样本，记录 URL、日期和浏览器版本。真实页面通过后，再把去敏的 DOM 样本纳入自动回归。

## 7. 推荐模块与数据契约

沿用原生 JavaScript、现有 Core 校验和模块组织，按职责拆分；本轮不需要先重写成框架或整体迁移 TypeScript。

| 模块 | 职责 |
| --- | --- |
| `shared/core.js` | 数据规范化、哈希输入、评分规则、配置迁移 |
| `content/*-dom-adapter.js` | 平台提取、稳定身份、提取质量和明确上下文关系 |
| `content/index.js` | 观察与去重、内容版本、后台任务订阅、页面生命周期 |
| `background/task-service.js`（新增） | 任务状态、合并请求、取消、配置版本和中断恢复 |
| `background/budget-service.js`（新增） | Provider 预算、持久计数、预留与冷却 |
| `background/cache-service.js` | 有界推理结果缓存、TTL、schema 与容量降级 |
| `background/library-service.js`（新增） | 收藏和反馈的独立保存、索引、删除、导出 |
| `providers/llm-router.js` | 非流式/流式适配与 AbortSignal 传递 |
| `content/overlay.js` | 任务所属面板、增量安全渲染、收藏/反馈操作 |

建议新增的核心记录如下，字段为设计草案：

```text
PostSnapshot: platform, postId, url, contentHash, text, quotedPost,
              extractionQuality, capturedAt, adapterVersion
AnalysisResult: contentHash, analysisConfigHash, dimensions,
                model, analyzedAt, schemaVersion
AnalysisJob: jobId, kind, cacheKey, contentHash, configVersion,
             status, createdAt, updatedAt, errorCode
SavedPost: id, snapshot, optionalAnalysis, optionalSources,
           tags, note, readState, savedAt
Feedback: postId, contentHash, profileId, value, reason, createdAt
```

消息协议建议：`START_ANALYSIS` 返回 `jobId`；`CANCEL_ANALYSIS` 取消订阅；`GET_JOB_STATUS` 恢复界面；流事件携带 `jobId/contentHash/sequence/status/delta`。不把 API Key、请求头或完整服务配置发给 content script。新增消息继续执行扩展页面/支持站点/主框架的来源校验，并校验任务所属标签页；掌握 `jobId` 不等于有权取消其他任务。

设置保存后由后台通知已连接页面，只下发 `toPublicConfig()` 允许的字段；保留启动与重连读取，逐步替代当前每 15 秒轮询完整公开配置。若采用连接端口，需做页面关闭与重连清理。

## 8. D1：诊断、测试与文档一致性

### 测试补齐

现有测试集中在 Core、Provider、Adapter 静态样本和 Overlay；`package.json` 的测试入口尚无完整 content/background 集成和自动浏览回归。

| 测试层 | 必测场景 |
| --- | --- |
| 单元 | 分层哈希、配置迁移、权重本地重算、SSE 分片、引用编号检查 |
| 后台集成 | 并发预算、重复任务、取消、禁用、worker 中断后恢复状态、存储失败 |
| 页面集成 | 长帖展开、虚拟列表节点复用、纯文本改变、文章移除、设置切换时响应竞态 |
| 浏览器回归 | 加载构建后的扩展、X/微博流程、多标签页、主机权限拒绝、离线、手动恢复 |
| 长会话 | 1,000 条模拟帖子、页面 Map 上限、缓存字节上限、上下文被淘汰后恢复 |

常规测试用固定样本和模拟接口，不消耗真实额度。真实接口测试保持显式运行；真实平台验收记录浏览器版本、页面类型、样本日期与结果，不将静态 fixture 成功视为线上兼容证明。

### 本地诊断面板

展示提取成功/失败计数、缓存命中、待处理数量、Provider 耗时、错误码、预算与存储用量。日志保留有界数量，只记录任务元信息；导出诊断默认不含正文、URL、兴趣、密钥和请求头。先做本地观测，无需远程遥测。

建议的验收目标：确定性用例中相同内容版本重复网络请求为 0；配置仅改变展示时请求为 0；所有任务都有终态或明确中断态；额度与并发用模拟请求验证不越界。页面性能阈值在选定设备建立基线后制定，当前不虚构改善百分比。

### 顺手修正的文档问题

- README 称默认 `host_permissions` 仅含 TypeSafe，实际 Manifest 还包含 Tavily，应同步说明。
- 原设计以 X 为主，当前已支持微博，应在后续实现提交中更新入口说明和验收范围。
- 自动浏览 Session 上限与全插件 API 上限分开描述。
- Tavily 额度文案和不同服务单价上线前重新核对，不把本次代码中的静态标价作为长期保证。
- `npm run check` 当前未包含缓存与自动浏览脚本的语法检查，可在对应开发时补齐。

## 9. 推荐分期与交付门槛

### 第一阶段：可信评分与可控请求

范围：A1–A4、D1 的关键回归。交付内容为内容版本协议、全局调度、深度任务缓存/取消、上下文恢复和清理。

发布门槛：正文变化不串用评分、多标签页不突破预算、禁用阻止新请求、worker 中断可见、长会话内存和存储受控。未过门槛不叠加批量摘要。

### 第二阶段：日常可用与内容留存

范围：B1、B2、C1、C2、C5 基础能力；B3 先交付阶段进度，流式按服务兼容情况独立完成。

交付体验：用户可直接调偏好、找到下一条值得读的内容、收藏和备注、导出带来源的阅读材料，并纠正不合适的筛选规则。

### 第三阶段：研究能力

范围：B3 的流式和追问、B4、C3、C5 动态页面增强；C4 按真实需求选做。C5 基础阶段先落实 B4 的来源标识契约，第三阶段再完善联网研究质量。

发布门槛：来源编号一致、降级状态明确、输入集合透明、联网开关与预算有效。支持新平台前先抽象 Adapter 契约并补齐该平台样本。

## 10. 第一轮可直接拆分的开发任务

- [ ] 明确推理/评分/展示三类配置，新增内容版本与缓存 schema。
- [ ] 修复同 ID 内容变化及晚到结果覆盖，补页面集成测试。
- [ ] 增加全局请求预算与队列优先级，覆盖多标签页和重试计数。
- [ ] 深度解析接入共享任务、缓存、取消和启用状态检查。
- [ ] 增加上下文缺失后的当前页恢复，不再只提示刷新。
- [ ] 增加提取完整度、删除节点清理与页面结果容量上限。
- [ ] 为存储配额失败提供降级，保留已成功的分析结果。
- [ ] 新增关键后台/自动浏览测试，记录浏览器回归步骤。
- [ ] 同步 README 权限、限额和数据保留说明。

建议第一批新增产品功能选择“本地收藏与阅读清单”和“可视化偏好设置”：直接复用现有评分与解析成果，且比立即扩平台、做云同步更容易形成可验证的用户价值。
